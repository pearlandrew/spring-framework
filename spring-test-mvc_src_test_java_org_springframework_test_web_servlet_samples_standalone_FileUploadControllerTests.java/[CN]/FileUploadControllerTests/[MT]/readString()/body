{
  MockMultipartFile file=new MockMultipartFile("file","orig",null,"bar".getBytes());
  try {
    standaloneSetup(new FileUploadController()).build().perform(fileUpload("/fileupload").file(file)).andExpect(model().attribute("message","File 'orig' uploaded successfully"));
  }
 catch (  NoSuchMethodError err) {
    err.printStackTrace();
  }
}
