{
  super(dataBufferFactory);
  Assert.notNull(response,"'response' must not be null");
  this.response=response;
  this.bodyProcessor=new ResponseBodyProcessor(response.getOutputStream(),bufferSize);
}
