{
  enableAsync=importMetadata.getAnnotationAttributes(EnableAsync.class.getName(),false);
  Assert.notNull(enableAsync,"@EnableAsync is not present on importing class " + importMetadata.getClassName());
}
