{
  Assert.notNull(getHttpHandler());
  this.reactorHandler=new HttpHandlerChannelHandler(getHttpHandler());
  this.reactorServer=(getPort() != -1 ? ReactiveNet.httpServer(getPort()) : ReactiveNet.httpServer());
}
