{
  return getParameterAnnotations(method,new AnnotationTypeFilter<T>(annotationType){
    @Override public boolean matches(    ParameterAnnotation<?> parameterAnnotation){
      return super.matches(parameterAnnotation) && parameterType.equals(parameterAnnotation.getParameterType());
    }
  }
);
}
