{
  Assert.notNull(payload,"payload must not be null");
  this.payload=payload;
  this.originalMessage=originalMessage;
  this.headerAccessor=(headerAccessor != null) ? headerAccessor : new MessageHeaderAccessor(originalMessage);
}
