{
  Object value=result.getResult();
  if (Void.TYPE.equals(result.getResultType().getRawClass())) {
    return Publishers.empty();
  }
  return (value instanceof Publisher ? (Publisher<Void>)value : this.conversionService.convert(value,Publisher.class));
}
