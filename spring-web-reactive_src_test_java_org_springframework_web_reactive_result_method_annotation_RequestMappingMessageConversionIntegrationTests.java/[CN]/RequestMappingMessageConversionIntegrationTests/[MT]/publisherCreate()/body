{
  ResponseEntity<Void> entity=performPost("/publisher-create",JSON,asList(new Person("Robert"),new Person("Marie")),null,Void.class);
  assertEquals(HttpStatus.OK,entity.getStatusCode());
  assertEquals(2,getApplicationContext().getBean(TestRestController.class).persons.size());
}
