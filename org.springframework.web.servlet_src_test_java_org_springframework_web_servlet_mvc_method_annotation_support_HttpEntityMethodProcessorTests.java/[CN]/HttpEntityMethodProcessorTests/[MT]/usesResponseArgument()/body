{
  assertFalse("HttpEntity parameter uses response argument",processor.usesResponseArgument(httpEntityParam));
  assertTrue("ResponseBody return type does not use response argument",processor.usesResponseArgument(responseEntityReturnValue));
  assertTrue("HttpEntity return type does not use response argument",processor.usesResponseArgument(httpEntityReturnValue));
}
