{
  if (!(o instanceof CollectionTypeDescriptor)) {
    return false;
  }
  CollectionTypeDescriptor type=(CollectionTypeDescriptor)o;
  return collectionType.equals(type.collectionType) && ObjectUtils.nullSafeEquals(elementType,type.elementType);
}
