{
  this.sockJsServiceRegistration=new SockJsServiceRegistration(this.defaultTaskScheduler);
  this.sockJsServiceRegistration.setInterceptors(this.interceptors.toArray(new HandshakeInterceptor[this.interceptors.size()]));
  return this.sockJsServiceRegistration;
}
