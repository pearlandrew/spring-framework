{
  Statement junitAfters=super.withAfters(frameworkMethod,testInstance,statement);
  return new RunSpringTestContextAfters(junitAfters,testInstance,frameworkMethod.getMethod(),getTestContextManager());
}
