{
  if (element == null) {
    throw new NullPointerException();
  }
  if (!done) {
    try {
      if (onNextInternal(element)) {
        subscription.request(1);
      }
 else {
        done();
      }
    }
 catch (    Throwable t) {
      done();
      onError(t);
    }
  }
}
