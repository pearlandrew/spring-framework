{
  long diff=greater.getTime() - lesser.getTime();
  long variance=Math.abs(expected - diff);
  assertTrue("expected approximate difference of " + expected + ", but actual difference was "+ diff,variance < 100);
}
