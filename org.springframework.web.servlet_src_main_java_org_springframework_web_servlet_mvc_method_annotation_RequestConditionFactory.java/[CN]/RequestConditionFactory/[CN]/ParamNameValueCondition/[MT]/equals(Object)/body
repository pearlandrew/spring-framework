{
  if (this == obj) {
    return true;
  }
  if (obj != null && obj instanceof ParamNameValueCondition) {
    ParamNameValueCondition other=(ParamNameValueCondition)obj;
    return ((this.name.equals(other.name)) && (this.value != null ? this.value.equals(other.value) : other.value == null) && this.isNegated == other.isNegated);
  }
  return false;
}
