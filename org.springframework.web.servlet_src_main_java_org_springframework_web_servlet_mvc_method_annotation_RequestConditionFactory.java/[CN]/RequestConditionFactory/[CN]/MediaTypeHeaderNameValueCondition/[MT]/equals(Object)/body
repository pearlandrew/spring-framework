{
  if (this == obj) {
    return true;
  }
  if (obj != null && obj instanceof MediaTypeHeaderNameValueCondition) {
    MediaTypeHeaderNameValueCondition other=(MediaTypeHeaderNameValueCondition)obj;
    return ((this.name.equalsIgnoreCase(other.name)) && (this.value != null ? this.value.equals(other.value) : other.value == null) && this.isNegated == other.isNegated);
  }
  return false;
}
