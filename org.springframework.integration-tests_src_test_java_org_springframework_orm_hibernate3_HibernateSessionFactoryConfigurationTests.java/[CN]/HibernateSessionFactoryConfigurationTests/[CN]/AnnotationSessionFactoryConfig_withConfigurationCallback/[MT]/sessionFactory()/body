{
  return new AnnotationSessionFactoryBuilder(dataSource()).setSchemaUpdate(true).doWithConfiguration(new HibernateConfigurationCallback<AnnotationConfiguration>(){
    public void configure(    AnnotationConfiguration configuration) throws Exception {
      configuration.addAnnotatedClass(Foo.class);
    }
  }
).buildSessionFactory();
}
