{
  if (method.getReturnType() != void.class && AnnotationUtils.findAnnotation(method,Async.class) != null) {
    throw new IllegalStateException("Asynchronous @EventListener method is not allowed to return reply events: " + method);
  }
}
