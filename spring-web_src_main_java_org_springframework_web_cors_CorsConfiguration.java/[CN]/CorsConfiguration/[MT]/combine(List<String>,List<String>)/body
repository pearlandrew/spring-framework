{
  if (other == null) {
    return source;
  }
  if (source == null || source.contains("*")) {
    return other;
  }
  List<String> combined=new ArrayList<String>(source);
  combined.addAll(other);
  return combined;
}
