{
  super("Unable to convert value \"" + ObjectUtils.nullSafeToString(value) + "\" from type '"+ sourceType.getName()+ "' to type '"+ targetType.getName()+ "'",cause);
  this.sourceType=sourceType;
  this.targetType=targetType;
  this.value=value;
}
