{
  Object multiple=getMultiple();
  if (multiple != null && (Boolean.TRUE.equals(multiple) || Boolean.parseBoolean(multiple.toString()) || "multiple".equals(multiple))) {
    return true;
  }
  return forceMultiple();
}
