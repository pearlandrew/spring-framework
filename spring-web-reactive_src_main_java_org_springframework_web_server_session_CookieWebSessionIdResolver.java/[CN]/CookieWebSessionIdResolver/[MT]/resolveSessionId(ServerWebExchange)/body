{
  HttpHeaders headers=exchange.getRequest().getHeaders();
  List<HttpCookie> cookies=headers.getCookies().get(getCookieName());
  return (CollectionUtils.isEmpty(cookies) ? Optional.empty() : Optional.of(cookies.get(0).getValue()));
}
