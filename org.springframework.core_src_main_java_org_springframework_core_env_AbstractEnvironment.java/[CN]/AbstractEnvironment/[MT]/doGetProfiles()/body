{
  if (explicitlySetProfiles)   return;
  String profiles=getProperty(ACTIVE_PROFILES_PROPERTY_NAME);
  if (profiles == null || profiles.equals("")) {
    return;
  }
  this.activeProfiles=commaDelimitedListToSet(trimAllWhitespace(profiles));
}
