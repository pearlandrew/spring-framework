{
  Object value=this.targetMap.get(key);
  if (value == null) {
    V newVal=create((K)key);
    if (newVal != null) {
      put((K)key,newVal);
    }
    return newVal;
  }
  return unwrapIfNecessary(value);
}
