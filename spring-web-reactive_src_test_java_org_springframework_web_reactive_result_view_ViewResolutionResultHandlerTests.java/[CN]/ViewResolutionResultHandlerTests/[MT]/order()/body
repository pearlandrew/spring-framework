{
  TestViewResolver resolver1=new TestViewResolver(new String[]{});
  TestViewResolver resolver2=new TestViewResolver(new String[]{});
  resolver1.setOrder(2);
  resolver2.setOrder(1);
  assertEquals(Arrays.asList(resolver2,resolver1),new ViewResolutionResultHandler(Arrays.asList(resolver1,resolver2),new DefaultConversionService()).getViewResolvers());
}
