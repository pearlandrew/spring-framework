{
  collectEvent(event);
  if (event.msg == null) {
    return null;
  }
 else   if (event.msg.equals("String")) {
    return event.msg;
  }
 else {
    return new TestEvent(this,event.getId(),event.msg);
  }
}
