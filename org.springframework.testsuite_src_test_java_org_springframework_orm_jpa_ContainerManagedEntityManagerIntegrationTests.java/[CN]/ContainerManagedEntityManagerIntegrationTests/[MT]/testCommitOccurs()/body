{
  EntityManager em=createContainerManagedEntityManager();
  doInstantiateAndSave(em);
  setComplete();
  endTransaction();
  assertEquals("Tx must have committed back",1,countRowsInTable("person"));
  deleteFromTables(new String[]{"person"});
}
