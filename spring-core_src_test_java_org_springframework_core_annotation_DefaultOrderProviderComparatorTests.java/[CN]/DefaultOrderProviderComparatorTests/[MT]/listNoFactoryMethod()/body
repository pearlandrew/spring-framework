{
  A a=new A();
  C c=new C(-50);
  B b=new B();
  List<?> items=Arrays.asList(a,c,b);
  comparator.sortList(items,new OrderProvider(){
    @Override public Integer getOrder(    Object obj){
      return null;
    }
  }
);
  assertOrder(items,c,a,b);
}
