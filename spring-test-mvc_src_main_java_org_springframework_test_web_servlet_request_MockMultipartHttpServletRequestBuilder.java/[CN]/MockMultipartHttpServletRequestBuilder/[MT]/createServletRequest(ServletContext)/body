{
  MockMultipartHttpServletRequest request=servlet3Present ? createServlet3Request() : new MockMultipartHttpServletRequest();
  for (  MockMultipartFile file : this.files) {
    request.addFile(file);
  }
  return request;
}
