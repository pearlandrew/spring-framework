{
  final int n=buffer.limit();
  for (int i=0; i < n; i++) {
    final byte b=buffer.get(i);
    if (b == '\n') {
      return i;
    }
 else     if (b == '\r' && i < n - 1 && buffer.get(i + 1) == '\n') {
      return i;
    }
  }
  return -1;
}
