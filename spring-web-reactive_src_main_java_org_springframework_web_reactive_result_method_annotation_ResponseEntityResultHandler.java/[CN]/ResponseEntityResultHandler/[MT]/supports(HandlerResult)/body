{
  ResolvableType returnType=result.getReturnValueType();
  return (HttpEntity.class.isAssignableFrom(returnType.getRawClass()) && !RequestEntity.class.isAssignableFrom(returnType.getRawClass()));
}
