{
  HashSet<Validator> validator=new HashSet<Validator>();
  for (  Class<? extends Validator> validatorType : factoryAnno.validatorTypes())   validator.add(getInstance(validatorType));
  return validator;
}
