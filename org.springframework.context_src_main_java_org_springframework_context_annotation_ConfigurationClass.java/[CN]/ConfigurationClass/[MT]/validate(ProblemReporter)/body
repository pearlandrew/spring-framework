{
  if (configurationAnnotation == null)   problemReporter.error(new NonAnnotatedConfigurationProblem());
  if (Modifier.isFinal(modifiers))   problemReporter.error(new FinalConfigurationProblem());
  for (  BeanMethod method : methods)   method.validate(problemReporter);
}
