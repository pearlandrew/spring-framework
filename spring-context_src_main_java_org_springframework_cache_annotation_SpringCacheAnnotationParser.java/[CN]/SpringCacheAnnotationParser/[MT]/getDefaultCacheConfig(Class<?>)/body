{
  final CacheConfig annotation=AnnotationUtils.getAnnotation(target,CacheConfig.class);
  if (annotation != null) {
    return new DefaultCacheConfig(annotation.cacheManager(),annotation.keyGenerator(),annotation.cacheNames());
  }
  return new DefaultCacheConfig();
}
