{
  logger.error("Unhandled error for " + session,ex);
  if (session.isOpen()) {
    try {
      session.close(CloseStatus.SERVER_ERROR);
    }
 catch (    Throwable t) {
      destroyHandler();
    }
  }
}
