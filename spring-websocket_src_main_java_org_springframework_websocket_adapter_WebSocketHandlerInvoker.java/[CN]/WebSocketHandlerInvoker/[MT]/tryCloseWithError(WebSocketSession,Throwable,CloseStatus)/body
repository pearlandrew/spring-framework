{
  if (exeption != null) {
    logger.error("Closing due to exception for " + session,exeption);
  }
  if (session.isOpen()) {
    try {
      session.close(CloseStatus.SERVER_ERROR);
    }
 catch (    Throwable t) {
      destroyHandler();
    }
  }
}
