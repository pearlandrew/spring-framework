{
  Assert.notNull(method,"Method must not be null");
  TypeVariableResolver variableResolver=(implementationClass != null ? forType(implementationClass).as(method.getDeclaringClass()) : null);
  return forType(method.getGenericReturnType(),variableResolver);
}
