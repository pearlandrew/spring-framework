{
  Assert.notNull(field,"Field must not be null");
  TypeVariableResolver variableResolver=(implementationClass != null ? forType(implementationClass).as(field.getDeclaringClass()) : null);
  return forType(field.getGenericType(),variableResolver);
}
