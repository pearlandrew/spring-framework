{
  if (index >= target.length()) {
    throw new SpelEvaluationException(getStartPosition(),SpelMessage.STRING_INDEX_OUT_OF_BOUNDS,target.length(),index);
  }
  return new TypedValue(String.valueOf(target.charAt(index)));
}
