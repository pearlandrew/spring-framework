{
  UserDestinationMessageHandler handler=new UserDestinationMessageHandler(brokerMessagingTemplate(),userDestinationResolver());
  webSocketRequestChannel().subscribe(handler);
  brokerChannel().subscribe(handler);
  return handler;
}
