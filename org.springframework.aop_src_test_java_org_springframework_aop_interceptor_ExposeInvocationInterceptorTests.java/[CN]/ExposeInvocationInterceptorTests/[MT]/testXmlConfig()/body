{
  XmlBeanFactory bf=new XmlBeanFactory(new ClassPathResource("org/springframework/aop/interceptor/exposeInvocation.xml"));
  ITestBean tb=(ITestBean)bf.getBean("proxy");
  String name="tony";
  tb.setName(name);
  assertEquals(name,tb.getName());
}
