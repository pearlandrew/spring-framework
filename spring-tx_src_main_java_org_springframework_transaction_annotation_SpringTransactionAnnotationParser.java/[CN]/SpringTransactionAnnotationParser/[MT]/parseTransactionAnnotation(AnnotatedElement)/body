{
  AnnotationAttributes ann=AnnotatedElementUtils.getAnnotationAttributes(ae,Transactional.class.getName(),false,false,false,false);
  if (ann != null) {
    return parseTransactionAnnotation(ann);
  }
 else {
    return null;
  }
}
