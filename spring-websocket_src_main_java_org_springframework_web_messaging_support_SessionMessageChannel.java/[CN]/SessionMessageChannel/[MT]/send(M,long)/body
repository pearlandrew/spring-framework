{
  PubSubHeaders headers=PubSubHeaders.fromMessageHeaders(message.getHeaders());
  headers.setSessionId(this.sessionId);
  @SuppressWarnings("unchecked") M messageToSend=(M)MessageBuilder.fromPayloadAndHeaders(message.getPayload(),headers.toMessageHeaders()).build();
  this.delegate.send(messageToSend);
  return true;
}
