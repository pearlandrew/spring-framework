{
  PubSubHeaders headers=PubSubHeaders.fromMessageHeaders(message.getHeaders());
  headers.setSessionId(this.sessionId);
  MessageBuilder<?> messageToSend=MessageBuilder.fromPayloadAndHeaders(message.getPayload(),headers.toMessageHeaders());
  this.delegate.send(messageToSend.build());
  return true;
}
