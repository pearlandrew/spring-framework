{
  XmlBeanDefinitionReader reader=new XmlBeanDefinitionReader(appContext);
  reader.loadBeanDefinitions(new ClassPathResource(fileName,AnnotationDrivenBeanDefinitionParserTests.class));
  appContext.refresh();
}
