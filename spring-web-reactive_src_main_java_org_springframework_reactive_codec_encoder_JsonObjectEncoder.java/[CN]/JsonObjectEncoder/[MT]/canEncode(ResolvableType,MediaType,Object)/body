{
  return mediaType.isCompatibleWith(MediaType.APPLICATION_JSON) && !(reactorPresent && Promise.class.isAssignableFrom(type.getRawClass())) && (rxJava1Present && Observable.class.isAssignableFrom(type.getRawClass()) || Publisher.class.isAssignableFrom(type.getRawClass()));
}
