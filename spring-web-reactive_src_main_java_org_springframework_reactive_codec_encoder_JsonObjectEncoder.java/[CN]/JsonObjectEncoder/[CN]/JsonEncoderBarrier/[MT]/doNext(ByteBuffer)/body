{
  count++;
  if (count == 1) {
    prev=next;
    doRequest(1);
    return;
  }
  ByteBuffer tmp=prev;
  prev=next;
  Buffer buffer=new Buffer();
  if (count == 2) {
    buffer.append("[");
  }
  buffer.append(tmp);
  buffer.append(",");
  buffer.flip();
  subscriber.onNext(buffer.byteBuffer());
}
