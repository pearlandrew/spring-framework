{
  if (StringUtils.hasText(configLocation)) {
    throw new UnsupportedOperationException("GenericWebApplicationContext does not support configLocation. Do " + "you still have an 'contextConfigLocations' init-param set?");
  }
}
