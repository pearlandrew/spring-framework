{
  if (!ObjectUtils.isEmpty(configLocations)) {
    throw new UnsupportedOperationException("GenericWebApplicationContext does not support configLocations. Do " + "you still have an 'contextConfigLocations' init-param set?");
  }
}
