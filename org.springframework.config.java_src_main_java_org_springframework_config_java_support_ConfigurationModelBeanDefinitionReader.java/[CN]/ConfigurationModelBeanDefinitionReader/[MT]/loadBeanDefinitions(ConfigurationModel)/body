{
  int initialBeanDefCount=beanFactory.getBeanDefinitionCount();
  for (  ConfigurationClass configClass : model.getAllConfigurationClasses())   loadBeanDefinitionsForConfigurationClass(configClass);
  return beanFactory.getBeanDefinitionCount() - initialBeanDefCount;
}
