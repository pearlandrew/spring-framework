{
  thrown.expect(IllegalArgumentException.class);
  thrown.expectMessage("Object of class [java.lang.Object] must be an instance " + "of interface java.util.Set");
  Assert.isInstanceOf(Set.class,new Object(),null);
}
