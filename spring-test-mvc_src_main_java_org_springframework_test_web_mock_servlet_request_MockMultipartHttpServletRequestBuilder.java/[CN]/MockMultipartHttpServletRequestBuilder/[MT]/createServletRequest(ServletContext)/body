{
  MockMultipartHttpServletRequest request=ClassUtils.hasMethod(ServletRequest.class,"startAsync") ? createServlet3Request() : new MockMultipartHttpServletRequest();
  for (  MockMultipartFile file : this.files) {
    request.addFile(file);
  }
  return request;
}
