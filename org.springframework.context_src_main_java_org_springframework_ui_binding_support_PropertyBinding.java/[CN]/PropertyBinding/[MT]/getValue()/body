{
  if (isDirty()) {
    if (bufferedValue != null) {
      return formatValue(bufferedValue);
    }
 else {
      return sourceValue;
    }
  }
 else {
    return formatValue(getModel().getValue());
  }
}
