{
  if (len > Integer.MAX_VALUE) {
    throw new IllegalArgumentException("Content-Length exceeds ShallowEtagHeaderFilter's maximum (" + Integer.MAX_VALUE + "): "+ len);
  }
  int lenInt=(int)len;
  this.content.resize(lenInt);
  this.contentLength=lenInt;
}
