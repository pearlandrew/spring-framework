{
  if (src == null) {
    return null;
  }
  if (src.length == 0) {
    return "";
  }
  if (delegate != null) {
    return new String(delegate.encode(src),DEFAULT_CHARSET);
  }
 else {
    return DatatypeConverter.printBase64Binary(src);
  }
}
