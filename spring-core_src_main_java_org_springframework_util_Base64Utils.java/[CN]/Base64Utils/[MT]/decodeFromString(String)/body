{
  if (src == null) {
    return null;
  }
  if (src.length() == 0) {
    return new byte[0];
  }
  if (delegate != null) {
    return delegate.decode(src.getBytes(DEFAULT_CHARSET));
  }
 else {
    return DatatypeConverter.parseBase64Binary(src);
  }
}
