{
  super.onError(t);
  if (!this.exchange.isResponseStarted() && this.exchange.getStatusCode() < INTERNAL_SERVER_ERROR.value()) {
    this.exchange.setStatusCode(INTERNAL_SERVER_ERROR.value());
  }
  logger.error("ResponseBodySubscriber error",t);
}
