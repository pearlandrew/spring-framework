{
  do {
    PooledByteBuffer pooledBuffer=this.exchange.getConnection().getByteBufferPool().allocate();
    ByteBuffer dst=pooledBuffer.getBuffer();
    copy(dst,src);
    dst.flip();
    this.buffers.add(pooledBuffer);
  }
 while (src.remaining() > 0);
}
