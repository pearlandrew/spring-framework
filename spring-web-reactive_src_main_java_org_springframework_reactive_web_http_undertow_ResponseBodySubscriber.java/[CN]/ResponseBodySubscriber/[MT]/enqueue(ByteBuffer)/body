{
  do {
    PooledByteBuffer pooledBuffer=exchange.getConnection().getByteBufferPool().allocate();
    ByteBuffer dst=pooledBuffer.getBuffer();
    copy(dst,src);
    dst.flip();
    buffers.add(pooledBuffer);
  }
 while (src.remaining() > 0);
}
