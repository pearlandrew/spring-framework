{
  MockPortletRequest request=new MockPortletRequest();
  Map attributes=new HashMap<Object,Object>();
  PortletUtils.exposeRequestAttributes(request,attributes);
  assertEquals("Obviously all of the entries in the supplied attributes Map are not being copied over (exposed)",attributes.size(),countElementsIn(request.getAttributeNames()));
}
