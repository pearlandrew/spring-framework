{
  ModelAndView<String> mav=ModelAndView.viewName("foo").modelAttribute("foo","bar").build();
  assertEquals(1,mav.model().size());
  assertTrue(mav.model().containsKey("foo"));
  assertEquals("bar",mav.model().get("foo"));
}
