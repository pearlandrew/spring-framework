{
  conversionService.addGenericConverter(Object.class,Object[].class,new ObjectToArrayConverter(conversionService));
  String[] result=conversionService.convert("",String[].class);
  assertEquals(0,result.length);
}
