{
  MockBody body=new MockBody(){
    @SuppressWarnings("unchecked") @Override public <S>Mono<S> convertToMono(    Class<? extends S> aClass){
      return (Mono<S>)mono;
    }
  }
;
  return build(body);
}
