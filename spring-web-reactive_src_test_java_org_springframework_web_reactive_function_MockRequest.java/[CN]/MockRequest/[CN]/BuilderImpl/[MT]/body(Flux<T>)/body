{
  MockBody body=new MockBody(){
    @SuppressWarnings("unchecked") @Override public <S>Flux<S> convertTo(    Class<? extends S> aClass){
      return (Flux<S>)flux;
    }
  }
;
  return build(body);
}
