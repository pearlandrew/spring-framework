{
  List<HttpMessageConverter<?>> converters=Collections.singletonList(new CodecHttpMessageConverter<>(new StringEncoder(),new StringDecoder()));
  return new ResponseBodyResultHandler(converters,new DefaultConversionService());
}
