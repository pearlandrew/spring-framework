{
  if (!done) {
    try {
      if (whenNext(element)) {
        subscription.request(1);
      }
 else {
        done();
      }
    }
 catch (    final Throwable t) {
      done();
      onError(t);
    }
  }
}
