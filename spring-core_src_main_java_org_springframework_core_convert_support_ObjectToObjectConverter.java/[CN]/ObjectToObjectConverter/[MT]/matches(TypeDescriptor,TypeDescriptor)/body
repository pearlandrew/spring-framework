{
  if (sourceType.getType() == targetType.getType()) {
    return false;
  }
  return (String.class == targetType.getType() ? hasFactoryConstructor(String.class,sourceType.getType()) : hasToMethodOrFactoryMethodOrConstructor(targetType.getType(),sourceType.getType()));
}
