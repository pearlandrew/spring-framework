{
  if (sourceType.getType().equals(targetType.getType())) {
    return false;
  }
  return (String.class.equals(targetType.getType()) ? hasFactoryConstructor(String.class,sourceType.getType()) : hasToMethodOrFactoryMethodOrConstructor(targetType.getType(),sourceType.getType()));
}
