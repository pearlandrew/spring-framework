{
  AnnotationConfigApplicationContext factory=new AnnotationConfigApplicationContext(OptionalAutowiredMethodConfig.class);
  assertTrue(factory.getBeansOfType(Colour.class).isEmpty());
  assertThat(factory.getBean(TestBean.class).getName(),equalTo(""));
}
