{
  Map<String,MappedInterceptor> beans=BeanFactoryUtils.beansOfTypeIncludingAncestors(beanFactory,MappedInterceptor.class,true,false);
  if (!beans.isEmpty()) {
    return new MappedInterceptors(beans.values().toArray(new MappedInterceptor[beans.size()]));
  }
 else {
    return null;
  }
}
