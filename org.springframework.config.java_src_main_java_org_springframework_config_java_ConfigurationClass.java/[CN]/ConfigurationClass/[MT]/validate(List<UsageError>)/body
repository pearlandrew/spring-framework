{
  if (metadata == null)   errors.add(new NonAnnotatedConfigurationError());
  if (Modifier.isFinal(modifiers))   errors.add(new FinalConfigurationError());
  for (  BeanMethod method : methods)   method.validate(errors);
}
