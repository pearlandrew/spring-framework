{
  if (output.isReady()) {
    logger.trace("Flushing");
    try {
      output.flush();
      flushOnNext=false;
    }
 catch (    IOException ignored) {
    }
  }
 else {
    flushOnNext=true;
  }
}
