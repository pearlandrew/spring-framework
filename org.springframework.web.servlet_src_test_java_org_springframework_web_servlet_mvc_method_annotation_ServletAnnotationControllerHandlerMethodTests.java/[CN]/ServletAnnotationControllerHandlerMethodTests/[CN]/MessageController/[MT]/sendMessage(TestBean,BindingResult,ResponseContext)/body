{
  if (result.hasErrors()) {
    responseContext.view("messages/new");
  }
 else {
    responseContext.redirect("/messages/{id}").uriVariable("id","1").queryParam("name","value").flashAttribute("successMessage","yay!");
  }
}
