{
  NettyDataBufferAllocator allocator=new NettyDataBufferAllocator(channel.delegate().alloc());
  ReactorServerHttpRequest adaptedRequest=new ReactorServerHttpRequest(channel,allocator);
  ReactorServerHttpResponse adaptedResponse=new ReactorServerHttpResponse(channel,allocator);
  return this.httpHandler.handle(adaptedRequest,adaptedResponse);
}
