{
  Assert.notNull(getHttpHandler());
  HttpHandler handler=new RequestHandlerAdapter(getHttpHandler());
  this.undertowServer=Undertow.builder().addHttpListener(getPort() != -1 ? getPort() : 8080,"localhost").setHandler(handler).build();
}
