{
  for (  SpelMapping mapping : this.mappings) {
    if (mapping instanceof FieldToFieldMapping && ((FieldToFieldMapping)mapping).getSourceField().startsWith(field)) {
      return true;
    }
  }
  return false;
}
