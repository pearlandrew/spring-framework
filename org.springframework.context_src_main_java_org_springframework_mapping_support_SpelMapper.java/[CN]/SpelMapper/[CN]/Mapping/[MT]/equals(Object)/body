{
  if (!(o instanceof Mapping)) {
    return false;
  }
  Mapping m=(Mapping)o;
  return source.getExpressionString().equals(m.source.getExpressionString()) && target.getExpressionString().equals(m.source.getExpressionString());
}
