{
  TestContextManager testContextManager=new TestContextManager(ExampleTest.class);
  assertEquals("Verifying the number of registered TestExecutionListeners for ExampleTest.",3,testContextManager.getTestExecutionListeners().size());
}
