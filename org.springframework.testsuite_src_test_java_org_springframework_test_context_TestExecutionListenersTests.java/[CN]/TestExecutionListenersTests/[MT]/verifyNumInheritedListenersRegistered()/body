{
  TestContextManager testContextManager=new TestContextManager(InheritedListenersExampleTest.class);
  assertEquals("Verifying the number of registered TestExecutionListeners for InheritedListenersExampleTest.",4,testContextManager.getTestExecutionListeners().size());
}
