{
  if (this.running) {
    try {
      this.reactorServer.shutdown().await();
      this.running=false;
    }
 catch (    InterruptedException ex) {
      throw new IllegalStateException(ex);
    }
  }
}
