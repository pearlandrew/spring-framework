{
  if (requestedExtensions != null) {
    if (logger.isDebugEnabled()) {
      logger.debug("Requested extension(s): " + requestedExtensions + ", supported extension(s): "+ supportedExtensions);
    }
  }
  return requestedExtensions;
}
