{
  try {
    return encodeUriComponents(scheme,authority,userinfo,host,port,path,query,fragment,DEFAULT_ENCODING);
  }
 catch (  UnsupportedEncodingException e) {
    throw new InternalError("'UTF-8' encoding not supported");
  }
}
