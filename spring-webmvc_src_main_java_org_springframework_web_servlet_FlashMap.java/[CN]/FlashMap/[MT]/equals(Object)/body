{
  if (this == obj) {
    return true;
  }
  if (obj != null && obj instanceof FlashMap) {
    FlashMap other=(FlashMap)obj;
    if (this.targetRequestParams.equals(other.targetRequestParams) && ObjectUtils.nullSafeEquals(this.targetRequestPath,other.targetRequestPath)) {
      return true;
    }
  }
  return false;
}
