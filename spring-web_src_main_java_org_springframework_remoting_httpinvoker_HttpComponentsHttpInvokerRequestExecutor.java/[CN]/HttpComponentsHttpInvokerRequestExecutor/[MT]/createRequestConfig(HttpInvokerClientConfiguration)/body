{
  if (this.connectionTimeout > 0 || this.connectionRequestTimeout > 0 || this.readTimeout > 0) {
    return RequestConfig.custom().setConnectTimeout(this.connectionTimeout).setConnectionRequestTimeout(this.connectionRequestTimeout).setSocketTimeout(this.readTimeout).build();
  }
 else {
    return RequestConfig.DEFAULT;
  }
}
