{
  String returnTypeName=returnType == null ? "<unknown>" : returnType.getSimpleName();
  return format("%s: name=%s; returnType=%s; modifiers=%d",getClass().getSimpleName(),name,returnTypeName,modifiers);
}
