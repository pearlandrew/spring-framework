{
  SerializationDelegate actualSerialization=this.storeByValue ? serialization : null;
  return new ConcurrentMapCache(name,new ConcurrentHashMap<Object,Object>(256),isAllowNullValues(),actualSerialization);
}
