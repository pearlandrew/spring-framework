{
  if (logger.isInfoEnabled()) {
    logger.info(ClassLoaderUtils.showClassLoaderHierarchy(invocation.getThis(),invocation.getThis().getClass().getName(),"\n","-"));
  }
  return invocation.proceed();
}
