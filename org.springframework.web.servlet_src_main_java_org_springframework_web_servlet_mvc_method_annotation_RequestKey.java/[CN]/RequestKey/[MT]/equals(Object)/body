{
  if (this == obj) {
    return true;
  }
  if (obj != null && obj instanceof RequestKey) {
    RequestKey other=(RequestKey)obj;
    return (this.patterns.equals(other.patterns) && this.methods.equals(other.methods) && this.paramsCondition.equals(other.paramsCondition)&& this.headersCondition.equals(other.headersCondition));
  }
  return false;
}
