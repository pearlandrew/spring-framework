{
  checkForSupportedMessageConverter(responseType);
  List<HttpMessageConverter<T>> supportedMessageConverters=getSupportedMessageConverters(responseType);
  return execute(url,HttpMethod.GET,new GetCallback<T>(responseType),new HttpMessageConverterExtractor<T>(responseType,supportedMessageConverters),urlVariables);
}
