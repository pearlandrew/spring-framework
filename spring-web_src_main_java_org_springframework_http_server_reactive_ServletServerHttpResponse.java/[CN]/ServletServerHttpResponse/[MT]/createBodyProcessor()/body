{
  try {
    registerListener();
    this.bodyProcessor=new ResponseBodyProcessor(this.response.getOutputStream(),this.bufferSize);
    return this.bodyProcessor;
  }
 catch (  IOException ex) {
    throw new UncheckedIOException(ex);
  }
}
