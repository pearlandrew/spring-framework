{
  return (List)execute(new UnitOfWorkCallback(){
    @Override protected Object doInUnitOfWork(    UnitOfWork unitOfWork) throws TopLinkException {
      return unitOfWork.registerAllObjects(entities);
    }
  }
);
}
