{
  TransactionManager tm=SessionFactoryBuilderSupport.getConfigTimeTransactionManager();
  if (tm == null) {
    throw new IllegalStateException("No JTA TransactionManager found - " + "'jtaTransactionManager' property must be set on SessionFactoryBuilder");
  }
  this.transactionManager=tm;
}
