{
  request.addParameter("name","value1");
  request.setContent("name=value2".getBytes("ISO-8859-1"));
  filter.doFilter(request,response,filterChain);
  assertEquals("Query string parameters should be listed ahead of form parameters","value1",filterChain.getRequest().getParameter("name"));
}
