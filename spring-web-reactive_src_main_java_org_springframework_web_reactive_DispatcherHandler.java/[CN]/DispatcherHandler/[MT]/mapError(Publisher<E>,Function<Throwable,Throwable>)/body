{
  return Publishers.lift(source,null,new BiConsumer<Throwable,Subscriber<? super E>>(){
    @Override public void accept(    Throwable throwable,    Subscriber<? super E> subscriber){
      subscriber.onError(function.apply(throwable));
    }
  }
,null);
}
