{
  DataBufferAllocator allocator=new DefaultDataBufferAllocator();
  return new ResponseBodyResultHandler(Arrays.asList(new ByteBufferEncoder(allocator),new StringEncoder(allocator),new JacksonJsonEncoder(allocator,new JsonObjectEncoder(allocator))),conversionService());
}
