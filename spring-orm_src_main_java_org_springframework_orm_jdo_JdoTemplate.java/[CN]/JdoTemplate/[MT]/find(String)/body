{
  return execute(new JdoCallback<Collection>(){
    @Override @SuppressWarnings("unchecked") public Collection doInJdo(    PersistenceManager pm) throws JDOException {
      Query query=pm.newQuery(queryString);
      prepareQuery(query);
      return (Collection)query.execute();
    }
  }
,true);
}
