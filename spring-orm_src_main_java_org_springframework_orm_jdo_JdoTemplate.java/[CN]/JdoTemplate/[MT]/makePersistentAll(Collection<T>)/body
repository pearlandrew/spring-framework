{
  return execute(new JdoCallback<Collection<T>>(){
    @Override public Collection<T> doInJdo(    PersistenceManager pm) throws JDOException {
      return pm.makePersistentAll(entities);
    }
  }
,true);
}
