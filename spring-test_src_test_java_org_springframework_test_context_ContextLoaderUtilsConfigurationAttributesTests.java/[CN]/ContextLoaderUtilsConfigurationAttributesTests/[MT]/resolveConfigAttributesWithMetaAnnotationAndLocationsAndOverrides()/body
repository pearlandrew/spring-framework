{
  List<ContextConfigurationAttributes> attributesList=resolveContextConfigurationAttributes(MetaLocationsFooWithOverrides.class);
  assertNotNull(attributesList);
  assertEquals(1,attributesList.size());
  assertAttributes(attributesList.get(0),MetaLocationsFooConfigWithOverrides.class,new String[]{"/foo.xml"},EMPTY_CLASS_ARRAY,ContextLoader.class,true);
}
