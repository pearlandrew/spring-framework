{
  Class<?> paramType=param.getParameterType();
  if (!paramType.getName().equals("java.util.Optional")) {
    throw new ServletRequestBindingException("Missing matrix variable '" + name + "' for method parameter type ["+ paramType.getName()+ "]");
  }
}
