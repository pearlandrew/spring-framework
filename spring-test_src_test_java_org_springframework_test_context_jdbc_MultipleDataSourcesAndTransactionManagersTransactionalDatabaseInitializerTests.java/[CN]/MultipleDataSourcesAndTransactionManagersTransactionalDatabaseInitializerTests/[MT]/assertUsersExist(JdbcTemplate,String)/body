{
  String query="select count(name) from user where name =?";
  for (  String user : users) {
    assertTrue("User [" + user + "] must exist.",jdbcTemplate.queryForObject(query,Integer.class,user).intValue() == 1);
  }
}
