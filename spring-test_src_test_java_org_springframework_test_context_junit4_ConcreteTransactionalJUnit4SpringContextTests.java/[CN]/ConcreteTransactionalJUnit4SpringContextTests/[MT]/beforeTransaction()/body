{
  assertEquals("Verifying the number of rows in the person table before a transactional test method.",1,countRowsInPersonTable(super.jdbcTemplate));
  assertEquals("Adding yoda",1,addPerson(super.jdbcTemplate,YODA));
}
