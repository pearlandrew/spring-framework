{
  assertTrue(this.listableBeanFactory.getBeanDefinitionCount() == 1);
  assertTrue("Should count 7 beans, not " + BeanFactoryUtils.countBeansIncludingAncestors(this.listableBeanFactory),BeanFactoryUtils.countBeansIncludingAncestors(this.listableBeanFactory) == 7);
}
