{
  if (index < 0 || index >= this._flightList.size()) {
    throw new IndexOutOfBoundsException("setFlight: Index value '" + index + "' not in range [0.."+ (this._flightList.size() - 1)+ "]");
  }
  this._flightList.set(index,vFlight);
}
