{
  Resource[] resources=resolver.getResources("java/net/URL.class");
  assertEquals(1,resources.length);
  @SuppressWarnings("deprecation") String expectedProtocol=(org.springframework.core.JdkVersion.getMajorJavaVersion() < org.springframework.core.JdkVersion.JAVA_19 ? "jar" : "jrt");
  assertProtocolAndFilename(resources[0],expectedProtocol,"URL.class");
}
