{
  HashSet<Validator> validator=new HashSet<Validator>();
  for (  Class<? extends Validator> validatorType : factoryAnno.validators())   validator.add(getInstance(validatorType));
  return validator;
}
