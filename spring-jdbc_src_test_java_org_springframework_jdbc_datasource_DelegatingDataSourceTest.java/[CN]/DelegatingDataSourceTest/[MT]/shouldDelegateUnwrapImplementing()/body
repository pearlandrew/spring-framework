{
  dataSource=new DelegatingDataSourceWithWrapper();
  EasyMock.replay(delegate);
  assertThat(dataSource.unwrap(ExampleWrapper.class),is((ExampleWrapper)dataSource));
  EasyMock.verify(delegate);
}
