{
  long currentTime=new Date().getTime();
  this.personController.setStubTimestamp(currentTime);
  this.mockMvc.perform(get("/persons/1").header("If-Modified-Since",currentTime)).andExpect(status().isNotModified()).andExpect(header().string("Last-Modified",nullValue()));
}
