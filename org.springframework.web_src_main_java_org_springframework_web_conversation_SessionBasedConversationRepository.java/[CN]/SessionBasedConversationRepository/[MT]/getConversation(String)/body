{
  RequestAttributes attributes=RequestContextHolder.currentRequestAttributes();
  Object conversation=attributes.getAttribute(getSessionAttributeNameForConversationId(id),RequestAttributes.SCOPE_GLOBAL_SESSION);
  Assert.isInstanceOf(Conversation.class,conversation);
  return (MutableConversation)conversation;
}
