{
  T anno=getAnnotation(annoType);
  if (anno == null) {
    throw new IllegalStateException(String.format("required annotation %s is not present on %s",annoType.getSimpleName(),this));
  }
  return anno;
}
