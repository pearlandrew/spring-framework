{
  if (declaringClass == null) {
    throw new IllegalStateException("declaringClass property is null. Call setDeclaringClass() before calling getLocation()");
  }
  return new Location(declaringClass.getLocation().getResource(),getSource());
}
