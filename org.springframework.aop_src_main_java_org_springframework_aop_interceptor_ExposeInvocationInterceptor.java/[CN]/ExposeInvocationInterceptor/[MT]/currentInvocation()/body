{
  MethodInvocation mi=(MethodInvocation)invocation.get();
  if (mi == null)   throw new IllegalStateException("No MethodInvocation found: Check that an AOP invocation is in progress, " + "and that the ExposeInvocationInterceptor is in the interceptor chain.");
  return mi;
}
