{
  TestBean target=new TestBean();
  ITestBean itb=(ITestBean)createProxy(target,getFixture().getAdvisors(new SingletonMetadataAwareAspectInstanceFactory(new NoDeclarePrecedenceShouldFail(),"someBean")),ITestBean.class);
  try {
    itb.getAge();
    fail();
  }
 catch (  IllegalStateException ex) {
  }
}
