{
  return (Mono.class.equals(type.getRawClass()) || Flux.class.equals(type.getRawClass()) || getConversionService().canConvert(Mono.class,type.getRawClass())|| getConversionService().canConvert(Flux.class,type.getRawClass()));
}
