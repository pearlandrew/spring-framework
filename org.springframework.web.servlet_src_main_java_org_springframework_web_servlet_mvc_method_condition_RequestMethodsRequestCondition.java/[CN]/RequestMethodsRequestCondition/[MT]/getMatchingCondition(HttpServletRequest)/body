{
  if (isEmpty()) {
    return this;
  }
 else {
    if (match(request)) {
      return new RequestMethodsRequestCondition(RequestMethod.valueOf(request.getMethod()));
    }
 else {
      return null;
    }
  }
}
