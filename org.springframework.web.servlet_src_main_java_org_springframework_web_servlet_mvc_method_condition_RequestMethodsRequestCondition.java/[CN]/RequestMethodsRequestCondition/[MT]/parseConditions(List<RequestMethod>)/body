{
  Set<RequestMethodRequestCondition> conditions=new LinkedHashSet<RequestMethodRequestCondition>(methods.size());
  for (  RequestMethod method : methods) {
    conditions.add(new RequestMethodRequestCondition(method));
  }
  return conditions;
}
