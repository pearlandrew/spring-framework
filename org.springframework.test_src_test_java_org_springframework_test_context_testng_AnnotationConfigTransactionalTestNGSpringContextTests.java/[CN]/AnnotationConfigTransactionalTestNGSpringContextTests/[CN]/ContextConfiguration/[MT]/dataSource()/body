{
  return new EmbeddedDatabaseBuilder().addScript("classpath:/org/springframework/test/context/testng/schema.sql").addScript("classpath:/org/springframework/test/context/testng/data.sql").build();
}
