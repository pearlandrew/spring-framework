{
  try {
    if (method.getName().equals("send") && args != null && completionListenerClass == method.getParameterTypes()[args.length - 1]) {
switch (args.length) {
case 2:
        return sendWithCompletionListenerMethod.invoke(target,args[0],deliveryMode,priority,timeToLive,args[1]);
case 3:
      return sendWithDestinationAndCompletionListenerMethod.invoke(target,args[0],args[1],deliveryMode,priority,timeToLive,args[2]);
case 5:
    return sendWithCompletionListenerMethod.invoke(target,args);
case 6:
  return sendWithDestinationAndCompletionListenerMethod.invoke(target,args);
}
}
return method.invoke(CachedMessageProducer.this,args);
}
 catch (InvocationTargetException ex) {
throw ex.getTargetException();
}
}
