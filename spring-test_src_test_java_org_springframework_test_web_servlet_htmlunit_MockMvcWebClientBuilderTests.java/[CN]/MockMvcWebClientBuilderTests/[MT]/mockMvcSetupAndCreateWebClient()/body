{
  Assume.group(TestGroup.PERFORMANCE);
  this.webClient=MockMvcWebClientBuilder.mockMvcSetup(this.mockMvc).createWebClient();
  assertMvcProcessed("http://localhost/test");
  assertDelegateProcessed("http://example.com/");
}
