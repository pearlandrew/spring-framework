{
  if (metadata == null)   problemReporter.error(new NonAnnotatedConfigurationError());
  if (Modifier.isFinal(modifiers))   problemReporter.error(new FinalConfigurationError());
  for (  BeanMethod method : methods)   method.validate(problemReporter);
}
