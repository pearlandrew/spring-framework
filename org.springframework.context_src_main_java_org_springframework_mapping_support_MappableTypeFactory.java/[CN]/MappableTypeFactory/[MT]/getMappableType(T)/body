{
  for (  MappableType type : mappableTypes) {
    if (type.isInstance(object)) {
      return type;
    }
  }
  throw new IllegalArgumentException("Object of type [" + object.getClass().getName() + "] not mappable - no suitable MappableType exists");
}
