{
  return (responseStatusCode >= 200 && responseStatusCode < 300) && HttpMethod.GET.name().equals(request.getMethod()) && (response.getHeader(HEADER_CACHE_CONTROL) == null || !response.getHeader(HEADER_CACHE_CONTROL).contains(DIRECTIVE_NO_STORE));
}
