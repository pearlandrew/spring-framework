{
  Collection<ApplicationListener> newColl=concurrent ? new CopyOnWriteArraySet<ApplicationListener>() : new LinkedHashSet<ApplicationListener>();
  newColl.addAll(this.applicationListeners);
  this.applicationListeners=newColl;
}
