{
  if (this.applicationContext != null && !hasBeanOfType(TilesConfigurer.class)) {
    throw new BeanInitializationException("It looks like you're trying to configure Tiles view resolution. " + "If not using @EnableWebMvc you must import WebMvcTilesConfiguration, " + "or declare your own TilesConfigurer bean.");
  }
  TilesRegistration registration=new TilesRegistration();
  this.viewResolvers.add(registration.getViewResolver());
  return registration;
}
