{
  RegionFactory rf=(RegionFactory)SessionFactoryBuilderSupport.getConfigTimeRegionFactory();
  if (rf == null) {
    throw new IllegalStateException("No Hibernate RegionFactory found - " + "'cacheRegionFactory' property must be set on SessionFactoryBuilder");
  }
  this.regionFactory=rf;
}
