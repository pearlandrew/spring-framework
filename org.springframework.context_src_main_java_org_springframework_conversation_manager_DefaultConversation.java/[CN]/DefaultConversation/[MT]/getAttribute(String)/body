{
  checkValidity();
  touch();
  Object value=attributes.getAttribute(name);
  if (value != null) {
    return value;
  }
  if (parent != null && !isolated) {
    return parent.getAttribute(name);
  }
  return null;
}
