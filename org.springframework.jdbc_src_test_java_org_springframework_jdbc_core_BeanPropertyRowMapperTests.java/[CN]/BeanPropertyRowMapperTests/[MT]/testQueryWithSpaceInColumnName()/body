{
  List result=jdbcTemplate3.query("select last_name as \"Last Name\", age, birth_date, balance from people",new BeanPropertyRowMapper(SpacePerson.class));
  assertEquals(1,result.size());
  SpacePerson bean=(SpacePerson)result.get(0);
  verifySpacePerson(bean);
}
