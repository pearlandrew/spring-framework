{
  if (beanInfoFactories == null) {
synchronized (beanInfoFactoriesMutex) {
      if (beanInfoFactories == null) {
        beanInfoFactories=Collections.synchronizedList(SpringFactoriesLoader.loadFactories(BeanInfoFactory.class,classLoader));
      }
    }
  }
  return beanInfoFactories;
}
