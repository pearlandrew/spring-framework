{
  @SuppressWarnings("serial") TestBean tb=new TestBean(){
    @SuppressWarnings("unused") StringBuilder name=new StringBuilder();
  }
;
  DirectFieldAccessor dfa=new DirectFieldAccessor(tb);
  assertEquals(StringBuilder.class,dfa.getPropertyType("name"));
}
