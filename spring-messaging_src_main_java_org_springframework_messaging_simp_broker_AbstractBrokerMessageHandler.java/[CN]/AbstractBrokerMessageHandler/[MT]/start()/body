{
synchronized (this.lifecycleMonitor) {
    if (logger.isInfoEnabled()) {
      logger.info("Starting...");
    }
    startInternal();
    this.running=true;
    if (logger.isInfoEnabled()) {
      logger.info("Started.");
    }
  }
}
