{
synchronized (this.lifecycleMonitor) {
    if (logger.isDebugEnabled()) {
      logger.debug("Starting");
    }
    startInternal();
    this.running=true;
    if (logger.isDebugEnabled()) {
      logger.debug("Started");
    }
  }
}
