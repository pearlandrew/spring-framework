{
  if (value instanceof Integer)   return ((char)((Integer)value).intValue());
  if (value instanceof String) {
    String s=(String)value;
    if (s.length() == 1)     return s.charAt(0);
  }
  return null;
}
