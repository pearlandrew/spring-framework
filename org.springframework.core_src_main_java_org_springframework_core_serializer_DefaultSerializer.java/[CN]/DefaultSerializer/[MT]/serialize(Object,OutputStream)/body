{
  Assert.isTrue(object instanceof Serializable,this.getClass().getName() + " requires a Serializable payload, but received [" + object.getClass().getName()+ "]");
  ObjectOutputStream objectOutputStream=new ObjectOutputStream(outputStream);
  objectOutputStream.writeObject(object);
  objectOutputStream.flush();
}
