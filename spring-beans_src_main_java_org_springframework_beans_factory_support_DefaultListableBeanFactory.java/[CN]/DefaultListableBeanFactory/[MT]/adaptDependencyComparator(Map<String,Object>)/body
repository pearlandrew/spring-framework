{
  if (this.dependencyComparator instanceof OrderComparator) {
    return ((OrderComparator)this.dependencyComparator).withSourceProvider(createFactoryAwareOrderSourceProvider(matchingBeans));
  }
 else {
    return this.dependencyComparator;
  }
}
