{
  if (!this.typeInfo.getTargetType().equals(targetType.getObjectType())) {
    return false;
  }
  if (this.converter instanceof ConditionalConversion) {
    return ((ConditionalConversion)this.converter).matches(sourceType,targetType);
  }
  return true;
}
